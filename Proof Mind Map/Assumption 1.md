We assume that at $t=0$, the agent lies outside of the desired shape of circumnavigation, at a distance of at least the minimum safe distance away, that is, $r(0)>r_s$, where $r_s$ is some constant satisfying

$$0<r_{min}<r_s<r^*(t)$$
**NEED TO PROPERLY DEFINE**
Since r varies should I just get a conservative estimate as the max distance between centroid and a given target plus some epsilon